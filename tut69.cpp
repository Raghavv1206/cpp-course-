/*

stl -> standard template library

library of what?
        |
        v
generic classes and function
why use stl -> reuse: well tested components

components of stl
->containers -> stores data
             -> use template classes
-> algorithms -> sorting 
              -> searching
              -> use template functions

->iterators -> object point sto an element in a container
            ->handled just like pointer
            -> connects algo. with container
*/